{
  "name": "lunargroove",
  "version": "1.0.0",
  "private": true,
  "description": "A frontend-only site deployed on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
